# Documentation site build dependencies (Material for MkDocs + i18n).
#   python3 -m venv .venv-docs
#   .venv-docs/bin/pip install -r docs/requirements.txt
#   .venv-docs/bin/mkdocs serve
mkdocs-material~=9.7
mkdocs-static-i18n~=1.3
# Read by tests/docs/run.py to compare the light and dark artwork pixel for
# pixel. Without it that check cannot run, and a check that quietly skips is
# worth less than no check at all.
pillow~=11.0
